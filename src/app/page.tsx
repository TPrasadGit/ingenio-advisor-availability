import AdvisorWrapper from "./wrapper/advisor";

export default function AdvisorPage() {

  return (
    <main className="flex min-h-screen flex-col items-center p-24">
      <AdvisorWrapper />
    </main>
  );
}
